'use client';
import {FormEvent,useState} from 'react';import Link from 'next/link';import Image from 'next/image';import {useRouter} from 'next/navigation';import {T} from '@/components/LanguageProvider';
export default function Login(){const [err,setErr]=useState(''),[busy,setBusy]=useState(false),[show,setShow]=useState(false);const r=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr('');try{const f=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const d=await res.json();if(!res.ok)return setErr(d.error||'Login failed');r.push('/menu');r.refresh()}catch{setErr('Could not connect. Please try again.')}finally{setBusy(false)}}return <main className="auth-shell"><section className="auth-card"><Image className="auth-logo" src="/tordher-bites-logo.svg" alt="Tordher Bites" width={300} height={74} priority/><h1><T>Welcome back</T></h1><p className="section-sub">Login to your Tordher Bites account.</p><form onSubmit={submit}><label className="form-label"><T>Email</T> <span className="required">*</span><input className="input" type="email" name="email" autoComplete="email" required placeholder="you@example.com"/></label><label className="form-label"><T>Password</T> <span className="required">*</span><div className="password-wrap"><input className="input" type={show?'text':'password'} name="password" autoComplete="current-password" required minLength={8} placeholder="••••••••"/><button type="button" className="eye-btn" onClick={()=>setShow(v=>!v)} aria-label={show?'Hide password':'Show password'}>{show?'◉':'◌'}</button></div></label>{err&&<p role="alert" className="form-error">{err}</p>}<button className="btn auth-submit" disabled={busy}>{busy?'Signing in…':<T>Login</T>}</button></form><p className="auth-switch"><T>New here?</T> <Link href="/signup"><T>Sign Up</T></Link></p></section></main>}
