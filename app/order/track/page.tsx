'use client';
import {FormEvent} from 'react';
import Link from 'next/link';
import {useRouter} from 'next/navigation';
export default function Track(){const router=useRouter();return <>
  <section className="commerce-hero track-hero"><div className="wrap commerce-hero-inner"><div><div className="eyebrow light">LIVE FROM THE KITCHEN</div><h1>KNOW WHAT'S<br/><span>HAPPENING.</span></h1><p>From “Received” to “Ready”, follow your Crispy Corner order without refreshing the page.</p></div><div className="track-live-orbit"><div className="orbit-ring"></div><div className="orbit-core"><span></span><b>LIVE</b><small>ORDER STATUS</small></div></div></div></section>
  <main className="track-premium-section"><div className="wrap track-premium-grid"><section className="track-copy"><div className="eyebrow">FAST. SIMPLE. LIVE.</div><h2>Your food has a journey.</h2><p>Enter the order number shown after checkout. We’ll take you directly to its live kitchen status.</p><div className="track-benefits"><div><b>01</b><span><strong>Enter your order ID</strong><small>It starts with CC-</small></span></div><div><b>02</b><span><strong>See the kitchen stage</strong><small>Updated automatically</small></span></div><div><b>03</b><span><strong>Know when it’s ready</strong><small>No manual refresh needed</small></span></div></div></section><form className="track-premium-card" onSubmit={(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const id=String(new FormData(e.currentTarget).get('id')||'').trim();if(id)router.push('/order/'+encodeURIComponent(id))}}><div className="track-card-top"><span className="live-dot"></span><b>LIVE TRACKING</b></div><h2>Find your order</h2><p>Your order number is on the confirmation screen after checkout.</p><label>ORDER NUMBER <span className="required">*</span><input name="id" className="track-input" placeholder="CC-MTZ..." autoComplete="off" required/></label><button className="btn track-submit">Track My Order →</button><small className="track-help">Can’t find your number? <Link href="/menu">Start a new order</Link></small></form></div></main>
</>}
