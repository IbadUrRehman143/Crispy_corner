import {prisma} from '@/lib/prisma';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const o=await prisma.order.findUnique({where:{orderNo:id},include:{items:true}});if(!o)return Response.json({error:'Order not found'},{status:404});return Response.json({orderNo:o.orderNo,status:o.status,total:o.total,orderType:o.orderType,createdAt:o.createdAt,estimatedMinutes:o.estimatedMinutes,deliveryFee:o.deliveryFee,items:o.items.map(i=>({name:i.name,qty:i.qty,unitPrice:i.unitPrice}))})}
