'use client';import {FormEvent,useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {CartItem,getCart,setCart} from '@/lib/cart';export default function Checkout(){const [items,setItems]=useState<CartItem[]>([]),[type,setType]=useState('Pickup'),[busy,setBusy]=useState(false),[err,setErr]=useState('');const r=useRouter();useEffect(()=>setItems(getCart()),[]);const total=items.reduce((s,x)=>s+x.price*x.qty,0);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr('');const f=new FormData(e.currentTarget);const res=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({customer:{name:f.get('name'),phone:f.get('phone'),address:f.get('address')||''},type,items:items.map(x=>({id:x.id,qty:x.qty}))})});const d=await res.json();setBusy(false);if(!res.ok)return setErr(d.error||'Order failed');setCart([]);r.push('/order/'+d.orderNo)}return <><section className="page-hero"><div className="wrap"><div className="eyebrow">Secure checkout</div><h1>Finish Your Order</h1><p>Just a few details and we’ll start preparing your food.</p></div></section><main className="wrap content"><div className="checkout-layout"><form className="card" onSubmit={submit}><h2 style={{marginTop:0}}>How would you like your order?</h2><div className="type-switch"><button type="button" className={type==='Pickup'?'active':''} onClick={()=>setType('Pickup')}>Pickup</button><button type="button" className={type==='Delivery'?'active':''} onClick={()=>setType('Delivery')}>Delivery</button></div><label className="form-label">Name <span className="required">*</span><input className="input" name="name" required minLength={2} placeholder="Your name"/></label><label className="form-label">Phone <span className="required">*</span><input className="input" name="phone" required placeholder="+923418290071"/></label>{type==='Delivery'&&<label className="form-label">Delivery address <span className="required">*</span><textarea className="input" name="address" required rows={4} placeholder="House / street / area"/></label>}{err&&<p style={{color:'var(--red)',fontWeight:800}}>{err}</p>}<button className="btn" style={{width:'100%',marginTop:8}} disabled={busy||!items.length}>{busy?'Placing order…':'Place Order • Rs '+total}</button></form><aside className="card summary"><div className="eyebrow">Your order</div><h2>{items.length} item{items.length===1?'':'s'}</h2>{items.map(x=><div className="summary-row" key={x.id}><span>{x.qty} × {x.name}</span><b>Rs {x.price*x.qty}</b></div>)}<div className="summary-row summary-total"><span>Total</span><span>Rs {total}</span></div><p className="section-sub" style={{fontSize:12}}>Final total is securely recalculated on the server. Payment: Cash on pickup/delivery.</p></aside></div></main></>}
