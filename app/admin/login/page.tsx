'use client';import Image from 'next/image';import Link from 'next/link';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';
export default function Login(){const r=useRouter(),[err,setErr]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr('');const f=new FormData(e.currentTarget);try{const x=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const d=await x.json();if(!x.ok)setErr(d.error||'Login failed');else r.push('/admin/orders')}catch{setErr('Could not connect. Please try again.')}finally{setBusy(false)}}return <main className="admin-login-page"><section className="admin-login-brand"><Link href="/" className="back-home">← Back to website</Link><div><Image src="/tordher-bites-logo.svg" alt="Tordher Bites" width={320} height={80}/><div className="eyebrow light">STAFF OPERATIONS</div><h1>Keep every order<br/><span>moving.</span></h1><p>One secure workspace for the kitchen queue, order status, menu availability and daily sales.</p></div></section><section className="admin-login-panel"><form onSubmit={submit} className="admin-login-form"><div className="eyebrow">SECURE ACCESS</div><h2>Welcome back</h2><p className="section-sub">Sign in with your Tordher Bites administrator account.</p><label className="form-label">Email <span className="required">*</span><input className="input" type="email" name="email" required autoComplete="email" placeholder="Admin email"/></label><label className="form-label">Password <span className="required">*</span><input className="input" type="password" name="password" required autoComplete="current-password" placeholder="••••••••••••"/></label>{err&&<p role="alert" className="form-error">{err}</p>}<button className="btn" style={{width:'100%'}} disabled={busy}>{busy?'Signing in…':'Open Dashboard →'}</button><small className="login-note">Authorized Tordher Bites staff only.</small></form></section></main>}
